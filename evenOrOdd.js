function evenOrOdd(num){
    //If the devision remainder is not equal to zero when divided by 2 it wll printout "That is a odd number"
    if (num % 2){
      console.log("That is a odd number")
    } 
    //If the devision remainder is not equal to zero when divided by 2 it wll printout "That is a even number"
    else{
      console.log("That is a even number")
    }
  } 
